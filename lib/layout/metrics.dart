import 'package:flutter/material.dart';

// the metrics class have the optimization purpose
class Metrics {
  // border radius config
  static const cardBorderRadius = BorderRadius.all(Radius.circular(15));
  static const inputBorderRadius = BorderRadius.all(Radius.circular(10));

  // padding and margins config
  static const page = EdgeInsets.all(16);
  static const defaultPadding = EdgeInsets.all(10);
}
